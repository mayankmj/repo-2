name: workflow_02
on:
  repository_dispatch:
    types: [Trigger Workflow]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3


